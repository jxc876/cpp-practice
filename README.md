# About

A project to practice C++ projects

Uses CMake to build & run

```shell
mkdir build
cmake -B build/ .
cmake --build build/
```

Then run the programs:

```shell
./build/main
./build/point
# etc...
```